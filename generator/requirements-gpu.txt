# ============================================================================
# SOUNDFLOW PREMIUM GPU REQUIREMENTS
# ============================================================================
# Use this file for GPU environments: Google Colab, Kaggle, RunPod, AWS
#
# Installation:
#   pip install -r requirements-gpu.txt
#
# Or for Colab/Kaggle (includes system deps):
#   !apt-get -qq update && apt-get -qq install -y ffmpeg
#   !pip install -q -r requirements-gpu.txt
# ============================================================================

# Core Dependencies
boto3>=1.34.0
botocore>=1.34.0
orjson>=3.10.0
pyyaml>=6.0.1
numpy>=1.26.0
scipy>=1.11.0

# AI/ML Libraries (GPU-optimized)
torch>=2.2.0
torchaudio>=2.2.0
transformers>=4.44.0
accelerate>=0.33.0

# Premium Audio Processing
audiocraft>=1.3.0
ffmpeg-python>=0.2.0

# Audio Utilities
pydub>=0.25.1
librosa>=0.10.0  # Optional: Advanced audio analysis

# Progress & Logging
tqdm>=4.66.0
