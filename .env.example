# =========================================
# SoundFlow AI — Environment Template
# Copy to .env (backend) and/or .env.local (frontend)
# =========================================

# -------------------------
# Frontend (Next.js / Vercel)
# -------------------------
# URL of the FastAPI backend (leave empty to use mock mode)
PYTHON_API_URL=

# Optional: set public app URL (useful for redirects, logging, etc.)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Optional: tier override for local testing (do NOT use in production)
# NEXT_PUBLIC_DEFAULT_TIER=free
# NEXT_PUBLIC_DEFAULT_TIER=premium


# -------------------------
# Backend (FastAPI)
# -------------------------
ENV=development
LOG_LEVEL=info

# CORS: comma-separated origins. Example:
# CORS_ALLOW_ORIGINS=http://localhost:3000,https://your-vercel-app.vercel.app
CORS_ALLOW_ORIGINS=http://localhost:3000

# ---- Object Storage (Cloudflare R2 / S3-compatible) ----
# Cloudflare R2 example:
# R2_ENDPOINT=https://<accountid>.r2.cloudflarestorage.com
R2_ENDPOINT=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET=

# Public base URL for serving objects (choose ONE approach):
# Option A: Cloudflare R2 Public Bucket URL or custom domain/CDN
R2_PUBLIC_BASE_URL=

# Optional: prefix paths inside the bucket
# Example: soundflow/
R2_PREFIX=soundflow

# ---- Signed URLs (premium) ----
# Secret used for signing URLs / tokens (rotate in production)
SIGNING_SECRET=change-me-in-production

# Signed URL TTL in seconds (e.g. 3600 = 1 hour)
SIGNED_URL_TTL_SEC=3600

# ---- Premium gating (stub for now) ----
# Later you can validate Stripe, JWT, or your auth provider.
# For now: allow simple header-based premium testing.
PREMIUM_BYPASS_TOKEN=


# -------------------------
# Generator (daily scripts)
# -------------------------
# Storage same as backend (reuse R2_*), plus:
GENERATOR_OUTPUT_DIR=./out

# Free daily generation presets
FREE_PRESETS_PATH=./prompts/free_presets.yaml

# Premium prompts (MusicGen/Stable Audio)
PREMIUM_PROMPTS_PATH=./prompts/premium_prompts.yaml

# Optional: ffmpeg binary (if not in PATH)
FFMPEG_BIN=ffmpeg


# -------------------------
# Optional: Remote GPU trigger (future)
# -------------------------
PREMIUM_TRIGGER_URL=
PREMIUM_TRIGGER_TOKEN=
